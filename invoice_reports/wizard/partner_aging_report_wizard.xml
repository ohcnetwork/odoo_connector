<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="partner_aged_report_view" model="ir.ui.view">
        <field name="name">partner.aged.excel.wizard</field>
        <field name="model">partner.aged.excel.wizard</field>
        <field name="arch" type="xml">
            <form string="Partner Aged Report">
                <group col="4">
                    <field name="date_from"/>
                    <field name="date_to"/>
                    <field name="report_type"/>
                    <field name="partners_ids" widget="many2many_tags"/>
                </group>
                <footer>
                    <button name="action_print_excel" string="Print Excel" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_partner_aged_report" model="ir.actions.act_window">
        <field name="name">Partner Aged Report</field>
        <field name="res_model">partner.aged.excel.wizard</field>
        <field name="type">ir.actions.act_window</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="partner_aged_report_view"/>
        <field name="target">new</field>
    </record>

    <menuitem id="menu_partner_aged_report"
              name="Partner Aged Report"
              parent="menu_nvoice_report_menu"
              sequence="7"
              action="action_partner_aged_report"/>
</odoo>
